org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveR8=false

# SMTP password for recipes@tyates.one (465/TLS)
# Set this in ~/.gradle/gradle.properties or via CI secrets, NOT in the repo
SMTP_PASSWORD=